This repository contains the Physical Twin component of a distributed Digital Twin architecture for a Battery Management System (BMS). Built in Unity, it simulates high-fidelity physical behaviors of battery cells (voltage, current, temperature) and exposes virtual sensor outputs. It acts as the core data producer, streaming real-time metrics over the network (via MQTT/WebSockets) to mimic a live physical asset.
